export interface TopCategory {
    id: number;
    name: string;
    slug: string;
    image: string
}

export interface TopCategoryResponse {
    id: number;
    name: string;
    slug: string;
    image: string;

}